export default {
  state: {},
  mutations: {},
  getters: {},
  actions: {
    // async fetchcityAreaTrees ({ commit, getters }, params) {
    //   const { cityAreaTrees } = getters.getHttps
    //   const { data } = await cityAreaTrees(params)
    //   if (data && data.code === 200) {
    //     return data
    //   } else {
    //     commit('setError', { status: true, message:  data.msg })
    //   }
    // }
  }
}
